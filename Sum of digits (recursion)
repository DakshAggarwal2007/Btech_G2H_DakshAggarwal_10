#include <stdio.h>
int sum(int a){
    if (a==0){
        return 0;
    }
    else{
        return ((a%10)+sum(a/10));
    }
}
int main() {
    int n;
    printf("Enter the number:");
    scanf("%d",&n);
    printf("Sum of digits of the number is %d:",sum(n));
    return 0;
}
